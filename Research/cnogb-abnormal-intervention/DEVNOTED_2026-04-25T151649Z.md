# DEVNOTED — 2026-04-25T151649Z

目的
- 檢查 `Research/cnogb-abnormal-intervention/src` 是否已清楚套用 `abnormal_transformer_detect.md` 的設計架構。
- 若未完整落地，記錄缺口與後續補齊方向。

結論
- `src/` 已經有明顯的分層雛形，且多數子模組名稱與設計文檔對齊。
- 但整體仍屬「部分實作 + 部分 placeholder + 尚未完整串接」狀態，還不能算是清晰、完整地套用設計架構。

已對上的部分
- 資料層：`src/data/loader.py`、`preprocessing.py`、`processing.py`、`sequence.py`、`dataset.py` 已對應 ingest / preprocess / sequence / dataloader。
- 特徵層：`src/features/preprocessor.py` 已對應 scaler / transformer preprocessing。
- 模型層：`src/models/transformer.py` 已有 `PositionalEncoding` 與 `TransformerPredictor`。
- 訓練層：`src/train/components.py`、`src/train/loop.py` 已有組件工廠與 epoch loop。
- 評估與解釋：`src/evaluate/eval.py`、`src/explain/permutation_importance.py`、`src/explain/uncertainty.py`、`src/utils/visualization.py` 已覆蓋部分 evaluate / explain / report 功能。
- 專案入口：`run.py`、`experiments/run_training.py`、`configs/training.yaml` 已出現基本 orchestration 雛形。

主要缺口
- 頂層模組仍以 placeholder 為主：`src/data.py`、`src/model.py`、`src/train.py`、`src/evaluate.py`、`src/explain.py` 仍沒有把設計文檔中的流程完整實作，只是薄封裝或 stub。
- 入口尚未真正串起來：`run.py` 仍是 TODO，沒有完成 data -> features -> train -> evaluate 的端到端流程。
- 實驗與報告鏈不完整：缺少設計文檔中提到的完整 `experiments/` 入口群、結果匯出腳本、報告生成模組與 outlier analysis 專用層。
- 測試與驗證不足：未看到對應的 `tests/` 或可直接驗證各步驟的最小測試資料與 smoke tests。
- 文件不一致：根目錄 `README.md` 仍描述得很簡化，與 `abnormal_transformer_detect.md` 的分層規劃不完全一致。

具體需要補齊的內容
- 把頂層 `src/*.py` 轉成真正的聚合入口，或明確標示為 compatibility layer，避免和子套件重複造成混淆。
- 補齊完整 pipeline orchestration：config loading、pair processing、feature preprocessing、sequence building、dataloader、training、evaluation、export、report。
- 補齊輸出 artefacts：checkpoint、metrics JSON、results CSV、prediction CSV、MC Dropout 結果、permutation importance 結果與圖檔。
- 補齊工程層文件：`tests/`、`scripts/`、`reports/` 或等價目錄，以及 README 中的執行步驟與目錄圖。

建議後續動作
1. 先把 `run.py` 改成真正的 orchestration entrypoint。
2. 釐清頂層 `src/*.py` 要保留為 facade 還是改成實作主入口。
3. 補一版最小可執行的 end-to-end smoke test，確認設計架構真的能跑通。

備註
- 這份筆記只記錄架構對照結果，不修改任何既有原始碼。